const createToDo = (title, desc, dueDate, priority, notes, checklist) => {
    return {
        title,
        desc,
        dueDate,
        priority,
        notes,
        checklist
    }
}

export default createToDo;